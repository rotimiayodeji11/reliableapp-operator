{{- if .Values.rbac.helpers.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "reliableapp-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "reliableapp-operator.resourceName" (dict "suffix" "reliableapp-viewer-role" "context" $) }}
rules:
- apiGroups:
  - ebay.training
  resources:
  - reliableapps
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ebay.training
  resources:
  - reliableapps/status
  verbs:
  - get
{{- end }}
